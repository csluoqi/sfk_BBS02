20160117要点:
a.项目中本想用肖文吉老师的视频中的jdbc封装的Dao,但是发现还不是很方便,现在又学习了Spring JDBCTemplate,
所以现在改用SrpingJdbctemplate
b.之前写的肖文吉老师的代码还留在项目中,可以参考使用
c.关于Dao层有没有必要使用接口,看了网上的评论,觉得还是有必要的,尽管这样有可能出现无效代码,按照我们组平哥
的说法,不要冗余的代码,但还有另外一个问题需要思考,就是代码冗余和代码的可拓展性.

20160118要点:
a.今天想用一下RESTController,结果发现这里又是一大快
并不是我想的那样所以这个在后期慢慢学习吧
b.有空将这个想项目中jdbcTemplate类写完这样,以后可以参考了

20160123要点:
a.建立两个js文件,一个用jquery,一个用原生的js来实现,学习js
b.这个后台操作可以参考cnblog的后台管理的js代码,很简单
20160124
a.暂时先不考虑利用ajax删除时后台需要做验证的问题
b.左侧导航栏选中一个链接之后链接的左边有一个灰色的原点,现在是静态的,现在需要坐成动态的
c.页面的标题是一样的,需要根据页面改成不一样的
d.页面中有相同的部分是否可以提出去
e.参考一下cnblog等网站他们的导航栏是如何实现的
d.在cnblogs中同一个模块下他们的的页面样式基本相同,标题用的是同一个标题
f.由于按照cnblogs的设计,同一个模块下他们的的页面样式基本相同,标题用的是同一个标题
g.发现一个规则,在跳转到新增的页面是命名为inputBook,输入完之后体提交到saveBook的连接
如果是修改,则为editBook->updateBook
h.明天做拦截器,设置编码为utf8
20160125
session和filter做session在线离线判断
使用taglib自定义标签做一个分页,上一页,下一页,第一页,最后一页的效果
学习使用ant编译,不过我发现了一个更方便的方法eclipse中ctl+B编译

20160128
后期添加一个计算访问量的工作
之前做的SpringMVC项目是吧书里面多个项目糅合在一起结果是一个都不能用了,好吧,以后再不这样偷懒了
以后不想再博客里面做复制站粘贴了,如果自己不去实践,复制过来又有上面意思
如果要收藏,那就直接收藏好了,也不用复制粘贴

20160218
由于Spring的验证比较麻烦,所以打算采用JSR303的校验,但是用了Srping的POST提交方式+表单对象,如果表单的数据
的类型不正确,直接就报400的错误,现在还是不能很好的控制Srping啊
做前端校验和防止重复提交

20160220
今天发现一个问题,就是那个不板块列表里面没有分页功能,这个bbs重点在于学习springmvc框架,所以视频里没有讲就不加了
20160322 

